#include <bits/stdc++.h>
#include<conio.h>

using namespace std;
int main(){
    ofstream file;
    file.open("data.txt",ios::out);
    file<<"Hello from New Process";
    file.close();
    while(1)cout<<".";
    return 0;
}